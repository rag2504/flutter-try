name: darshan
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+4
  path: ^1.8.0
  path_provider: ^2.1.2  # Required for working with file paths
  flutter_platform_widgets: ^3.0.0  # Helps in handling platform differences

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0  # Updated to latest version for best practices

flutter:
  uses-material-design: true